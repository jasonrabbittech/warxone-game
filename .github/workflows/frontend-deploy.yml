name: Frontend Deploy

# EdgeOne Pages auto-deploys on Git push to main/develop branches.
# EdgeOne reads edgeone.json at repo root for build configuration:
#   - installCommand: "cd frontend && npm install"
#   - buildCommand: "cd frontend && npm run build"
#   - outputDirectory: "./frontend/dist"
# This workflow serves as a build validation gate and artifact archive.
# It does NOT deploy directly - EdgeOne handles deployment automatically.

on:
  push:
    branches: [main, develop]
    paths: ['frontend/**']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Validate JS syntax
        run: |
          find dist/assets -name '*.js' -exec node --check {} \;

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 7

      - name: Summary
        run: |
          echo "## Build Output" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "EdgeOne Pages will auto-deploy from this push." >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
          ls -lh dist/assets/ >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
